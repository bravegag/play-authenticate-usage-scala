package controllers

object ApplicationKeys {
  //-------------------------------------------------------------------
  val FlashMessage = "message"
  val FlashError = "error"
  val UserRole = "user"
}
